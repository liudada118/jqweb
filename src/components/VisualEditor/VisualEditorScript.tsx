'use client'

/**
 * This component is no longer needed as the inject script is now handled
 * by VisualEditorLayoutClient. Kept as empty export for backward compatibility.
 */
export function VisualEditorScript() {
  return null
}
